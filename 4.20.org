* Question
:PROPERTIES:
:header-args:scheme: :eval no
:END:
Because internal definitions look sequential but are actually simultaneous, some people prefer to avoid them entirely, and use the special form ~letrec~ instead. ~Letrec~ looks like ~let~, so it is not surprising that the variables it binds are bound simultaneously and have the same scope as each other. The sample procedure ~f~ above can be written without internal definitions, but with exactly the same meaning, as

#+begin_src scheme
(define (f x)
  (letrec ((even?
            (lambda (n)
              (if (= n 0)
                  true
                  (odd? (- n 1)))))
           (odd?
            (lambda (n)
              (if (= n 0)
                  false
                  (even? (- n 1))))))
    <rest of body of f>))
#+end_src

~Letrec~ expressions, which have the form

#+begin_src scheme
(letrec ((<var1> <exp1>) ... (<varn> <expn>))
  <body>)
#+end_src

are a variation on ~let~ in which the expressions ~<exp_k>~ that provide the initial values for the variables ~<var_k>~ are evaluated in an environment that includes all the ~letrec~ bindings. This permits recursion in the bindings, such as the mutual recursion of ~even?~ and ~odd?~ in the example above, or the evaluation of 10 factorial with

#+begin_src scheme
(letrec ((fact
          (lambda (n)
            (if (= n 1)
                1
                (* n (fact (- n 1)))))))
  (fact 10))
#+end_src

a. Implement ~letrec~ as a derived expression, by transforming a ~letrec~ expression into a ~let~ expression as shown in the text above or in exercise 4.18. That is, the ~letrec~ variables should be created with a ~let~ and then be assigned their values with ~set!~.

b. Louis Reasoner is confused by all this fuss about internal definitions. The way he sees it, if you don't like to use ~define~ inside a procedure, you can just use ~let~. Illustrate what is loose about his reasoning by drawing an environment diagram that shows the environment in which the ~<rest of body of f>~ is evaluated during evaluation of the expression ~(f 5)~, with ~f~ defined as in this exercise. Draw an environment diagram for the same evaluation, but with ~let~ in place of ~letrec~ in the definition of ~f~.

* Answer
** a.
*** Implementation
:PROPERTIES:
:header-args:scheme: :eval no
:END:
#+begin_src scheme :tangle 4.20.mceval.eval.letrec.scm
  (define (install-letrec-package)
    (put 'eval 'letrec
         (lambda (exp env)
           (eval (letrec->let exp) env)))
    'ok)

  (define (letrec->let exp)
    (let* ((defs (map (lambda (x) (list x '(quote *unassigned*)))
                      (let-args exp)))
           (assignments (let loop ((args (let-args exp))
                                   (vals (let-vals exp)))
                          (if (null? args) '()
                              (cons (make-assignment (car args)
                                                     (car vals))
                                    (loop (cdr args)
                                          (cdr vals))))))
           (body (append assignments (let-body exp))))
      (make-let defs body)))
#+end_src

#+begin_src scheme :tangle 4.20.mceval.scm
  ;; Inherit
  (load "4.17.mceval.scm")
  ;; Expression types
  (load "4.20.mceval.eval.letrec.scm")
  ;; Setup
  (install-letrec-package)
#+end_src

*** Test
#+begin_src scheme :exports both
  ;; Load
  (load "4.20.mceval.scm")
  ;; Test
  (mc-test '(begin
              (define (f x)
                (letrec ((even?
                          (lambda (n)
                            (if (= n 0)
                                true
                                (odd? (- n 1)))))
                         (odd?
                          (lambda (n)
                            (if (= n 0)
                                false
                                (even? (- n 1))))))
                  (even? x)))
              (f 20))
           true)
#+end_src

#+RESULTS:
: ok

** b.
:PROPERTIES:
:header-args:scheme: :eval no
:END:
Louis does not see that ~let~ clause definitions lie outside their own scope. ~letrec~ solves this problem by assigning the definition within the internal block.

#+begin_src scheme
  (let ((x <exp>)) ...)
#+end_src

#+begin_src scheme
  ((lambda (x) ...) <exp>) ;; <exp> cannot refer to itself!
#+end_src

*** ~letrec~
#+begin_src dot
  digraph {
    rankdir=RL

    subgraph {
      node[shape=record]
      f0 [label="<t>the-global-environment|..."]
      f1 [label="<t>frame-1|x: 5"]
      f2 [label="<t>frame-2|<v1>even?|<v2>odd?"]
    }

    subgraph {
      node[label="" shape=doublecircle width=0.2]
      subgraph {
        rank=same
        p1
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p1_def[label="(lambda (x)\l  (letrec ((even? *un*)\l           (odd? *un*))\l    (set! even? ...)\l    (set! odd? ...)\l...))"]
        }
      }
      subgraph {
        rank=same
        p2
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p2_def[label="(lambda (even? odd?)\l  (set! even? ...)\l  (set! odd? ...)\l...))"]
        }
      }
      subgraph {
        rank=same
        p3
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p3_def[label="(lambda (n)\l...)"]
        }
        p3 -> p3_def
      }
      subgraph {
        rank=same
        p4
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p4_def[label="(lambda (n)\l...)"]
        }
        p4 -> p4_def
      }
    }

    f2 -> f1 -> f0

    f2:v1 -> p3
    f2:v2 -> p4
    p3 -> f2:t [color=green]
    p4 -> f2:t [color=green]

    p1 -> f0:t
    p1 -> f1:t [style=dashed]
    p1 -> p1_def

    p2 -> f1:t
    p2 -> f2:t [style=dashed]
    p2 -> p2_def
  }
#+end_src

*** ~let~
#+begin_src dot
  digraph {
    rankdir=RL

    subgraph {
      node[shape=record]
      f0 [label="<t>the-global-environment|..."]
      f1 [label="<t>frame-1|x: 5"]
      f2 [label="<t>frame-2|<v1>even?|<v2>odd?"]
    }

    subgraph {
      node[label="" shape=doublecircle width=0.2]
      subgraph {
        rank=same
        p1
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p1_def[label="(lambda (x)\l  (let ((even? ...)\l        (odd? ...))\l...))"]
        }
      }
      subgraph {
        rank=same
        p2
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p2_def[label="(lambda (even? odd?)\l...))"]
        }
      }
      subgraph {
        rank=same
        p3
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p3_def[label="(lambda (n)\l...)"]
        }
        p3 -> p3_def
      }
      subgraph {
        rank=same
        p4
        subgraph {
          node[shape=plaintext fontname="Courier New" fontsize=10]
          p4_def[label="(lambda (n)\l...)"]
        }
        p4 -> p4_def
      }
    }

    f2 -> f1 -> f0

    f2:v1 -> p3
    f2:v2 -> p4
    p3 -> f1:t [color=red]
    p4 -> f1:t [color=red]

    p1 -> f0:t
    p1 -> f1:t [style=dashed]
    p1 -> p1_def

    p2 -> f1:t
    p2 -> f2:t [style=dashed]
    p2 -> p2_def
  }
#+end_src
